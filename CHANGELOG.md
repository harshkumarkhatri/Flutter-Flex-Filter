## 1.0.0

- Initial stable release of `flutter_flex_filter`.
- Added `QueryParameterBuilder` for building filter, sort, search, and pagination query params.
- Added logical and relational operand helpers for type-safe query construction.
- Added support for report flags, soft-delete flags, and custom parameters.
