name: flutter_flex_filter
description: "A Flutter package that provides flexible filtering functionality for lists with a user-friendly interface."
version: 1.0.0
homepage: https://github.com/harshkumarkhatri/Flutter-Flex-Filter

environment:
  sdk: ^3.5.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
